services:
  community-app:
    container_name: container_name
    build: 
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - 3001:3001
    env_file:
      - .env
